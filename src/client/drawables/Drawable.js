// Drawable with a position
"use strict";

class Drawable {
	update(dt) {} // eslint-disable-line no-unused-vars
	draw(ctx, x, y, w, h) {} // eslint-disable-line no-unused-vars
}

module.exports = Drawable;
